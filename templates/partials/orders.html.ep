% if ($orders->count) {
<div class="list-group">
  % while (my $order = $orders->next) {
  % my $ymd = $order->create_date->ymd;
  <a href="<%= url_for('/orders/' . $order->id) %>" class="list-group-item list-group-item-success">
    <h4><%= $ymd %></h4>
    <p>
      session 에만 넣을 것이 아니라 order_detail 에 넣어두자
    </p>
    <p>
      상태에 따라서 배경색을 달리해주자
    </p>
  </a>
  % }
</div>
% } else {
<p>대여이력이 없습니다</p>
% }
